/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['res.cloudinary.com' ,'images.pexels.com',"lh3.googleusercontent.com", 'avatars.githubusercontent.com']
    }
};

export default nextConfig;
